Place your application icon here to have it embedded into the Windows MSI/EXE.

- Expected file: `assets/logo.ico` (recommended sizes: 256x256 + 48x48 + 32x32)
- If missing, the build scripts will generate a simple fallback icon automatically.

